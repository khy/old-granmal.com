# First of all:
# 1. run `play dist`
# 2. replace ::version:: with current app version

FROM granmal/base:0.0.1

ENV VERSION ::version::
WORKDIR /app

ADD target/universal/granmal-$VERSION.zip /app/
RUN unzip granmal-$VERSION.zip
CMD granmal-$VERSION/bin/granmal
